# These values are used only by the Netlify serverless function.
# Never commit your real key and never put it inside client-side JavaScript.
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-5.6-luna
